# supabase/RLS.md — AUTO-GENERATED by scripts/db_inspect.py. Do not hand-edit.
Read-only mirror of live RLS state. Regenerate: `make db-inspect`.

## admin_audit_log  (RLS: ENABLED)
- (no policies)

## admin_users  (RLS: ENABLED)
- (no policies)

## agents  (RLS: ENABLED)
- policy tenant_isolation_agents [SELECT] USING ((tenant_id = ((auth.jwt() ->> 'tenant_id'::text))::uuid))

## mint_rejections  (RLS: ENABLED)
- (no policies)

## quota_state  (RLS: ENABLED)
- policy tenant_isolation_quota [SELECT] USING ((tenant_id = ((auth.jwt() ->> 'tenant_id'::text))::uuid))

## sessions  (RLS: ENABLED)
- policy tenant_isolation_sessions [SELECT] USING ((tenant_id = ((auth.jwt() ->> 'tenant_id'::text))::uuid))

## tenants  (RLS: ENABLED)
- policy tenant_isolation_tenants [SELECT] USING ((id = ((auth.jwt() ->> 'tenant_id'::text))::uuid))

## usage_events  (RLS: ENABLED)
- policy tenant_isolation_usage [SELECT] USING ((tenant_id = ((auth.jwt() ->> 'tenant_id'::text))::uuid))

## used_nonces  (RLS: ENABLED)
- (no policies)

## voices  (RLS: ENABLED)
- policy voices_read_all [SELECT] USING (enabled)

