[pytest]
# `pipecat_stubs` does not exist in this checkout (nor do services/, config/, bench/) — it is a
# leftover from the pre-port Pipecat repo. Harmless to pytest, but listed here so the next reader
# does not go looking for a directory that was never here.
pythonpath = . pipecat_stubs
testpaths = tests

# EXPLICIT WHITELIST, not a glob — a file absent from this line does not run in `make gate`.
#
# Removed 2026-07-27 (ADR-030, human sign-off recorded in state/BLOCKERS.md):
#   test_tts.py     -> imports `services.tts_cache`; `services/` does not exist
#   test_harness.py -> imports `config` via tests/helpers.py; `config/` does not exist
# Both are the retired CER harness. Files kept on disk deliberately — excluded, not deleted, so
# the evidence survives if the harness is ever revived. Their import errors were the entire gate
# blocker: ONE collection error interrupts the whole run, so `make test` reported nothing at all
# rather than "57 passed".
#
# Added 2026-07-27 — each measured green before being added (19 passed, 0 failed):
#   test_phase4_portal_api.py   the tenant portal API = the dashboard's backend, incl. the
#                               stale-session regression test guarding the "13 live calls" bug
#   test_machine_agent_api.py   HMAC-signed /machine/agents surface
#   test_phase2.py              DB secret provider + admin secret rotation
#   test_phase4_voice_picker.py voice catalogue
# Until then the gate ran the two DEAD files and skipped these 19 live ones.
#
# Deliberately NOT added:
#   test_phase8_prod.py  imports the missing `bench` package — would reintroduce a collection error
#   any tests/*_live.py  these spend real money on Uplift/Gemini/LiveKit; never in the gate
#
# Added 2026-07-31 (ADR-036, docs/UKASHA_AGENT_FACING_MULTIPLE_PROVIDERS_PLAN.md Phase 1):
#   test_provider_registry_schema.py   0016/0017 backfill correctness, 0006's trigger still
#                                       enforced, new rollout_state CHECK constraint
# Added 2026-07-31 (ADR-036, Phase 2):
#   test_provider_registry.py          worker/providers/ registry vs legacy factories.py parity,
#                                       byte-identical fixture-mode Urdu regression, tts_voice_id
#                                       fallback, typed-error-not-silent-fallback
# Added 2026-08-01 (ADR-036, Phase 3):
#   test_provider_validation.py        resolve_agent_provider_fields error codes, voice_id/
#                                       tts_voice_id sync + priority rule, update-current defaults
#   test_sdk_parity.py                 sdk-server/client-submission byte-identity + new fields
# Added 2026-08-01 (ADR-036, Phase 4):
#   test_provider_capabilities.py      get_public_capabilities shape, only-enabled filtering,
#                                       en absent (nothing enabled yet), no planned/disabled leaks
# Added 2026-08-01 (ADR-036, Phase 5):
#   test_english_language.py           en+gladia STT builds correctly, capabilities API shows
#                                       en stt/llm enabled with no tts, full en agent creation
#                                       still cleanly rejected pending a Phase 6 TTS vendor
# Added 2026-08-01 (ADR-036, Phase 6a):
#   test_deepgram_stt.py                deepgram.STT constructs for en (skips cleanly if
#                                        DEEPGRAM_API_KEY absent), still rejected via tenant
#                                        routes while rollout_state is `testing`
# Added 2026-08-01 (ADR-036, Phase 6b):
#   test_groq_llm.py                    groq.LLM constructs for en (skips cleanly if
#                                        GROQ_API_KEY absent), still rejected via tenant routes
#                                        while rollout_state is `testing`, never selectable for ur
# Added 2026-08-02 (ADR-036, Phase 6c):
#   test_cartesia_tts.py                 cartesia.TTS constructs for en (skips cleanly if
#                                         CARTESIA_API_KEY absent), voice-ID resolution
#                                         (internal slug -> real vendor ID), now enabled + full
#                                         end-to-end resolution succeeds
# Added 2026-08-02 (ADR-036, Phase 6d):
#   test_elevenlabs_tts.py                elevenlabs.TTS constructs for en (skips cleanly if
#                                          ELEVEN_API_KEY absent), now enabled + full end-to-end
#                                          resolution succeeds
# Added 2026-08-02 (ADR-036, Phase 6e):
#   test_fish_audio_tts.py                 fishaudio.TTS constructs for en (skips cleanly if
#                                           FISH_API_KEY absent; no `language` kwarg exists on
#                                           this plugin, a real difference from Cartesia/
#                                           ElevenLabs), still rejected via tenant routes while
#                                           rollout_state is `testing`. Provider itself BLOCKED on
#                                           account funding (402 Payment Required) —
#                                           state/BLOCKERS.md::BLOCK-FISHAUDIO — code/tests still
#                                           green, just never promoted past `testing`.
# Added 2026-08-02 (ADR-036, Phase 6f):
#   test_rime_tts.py                       rime.TTS constructs for en (skips cleanly if
#                                           RIME_API_KEY absent; voice kwarg is `speaker`, lang
#                                           kwarg uses 3-letter codes — real differences from
#                                           every other TTS adapter), still rejected via tenant
#                                           routes while rollout_state is `testing`
python_files = test_isolation.py test_mint.py test_worker.py test_admin.py test_phase0.py test_host_backend_contract.py test_phase2.py test_phase4_portal_api.py test_phase4_voice_picker.py test_machine_agent_api.py test_provider_registry_schema.py test_provider_registry.py test_provider_validation.py test_sdk_parity.py test_provider_capabilities.py test_english_language.py test_deepgram_stt.py test_groq_llm.py test_cartesia_tts.py test_elevenlabs_tts.py test_fish_audio_tts.py test_rime_tts.py conftest.py
