Run /ponytail-debt (or grep -rn 'ponytail:' --include='*.py' --include='*.ts' .).
For each deferred shortcut: is it still acceptable? Promote anything load-bearing into a task
in state/PROGRESS.md, or accept it explicitly in docs/40-ADR.md.
Required at every phase gate. "Later" must not become "never".
